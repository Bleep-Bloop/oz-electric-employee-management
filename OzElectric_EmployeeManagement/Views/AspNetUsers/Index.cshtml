@model IEnumerable<OzElectric_EmployeeManagement.Models.AspNetUser>



@{
    ViewBag.Title = "Users";

}

<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Create New <span class="fa fa-plus fa-lg" aria-hidden="true"></span></a>
</p>

@if (HttpContext.Current.User.IsInRole("Admin"))
{

    <p>
        <a href="@Url.Action("ExportToCSV")" class="btn btn-primary">Export CSV <span class="fa fa-download fa-lg" aria-hidden="true"></span></a>
    </p>
    <p>
        <a href="@Url.Action("ExportToExcel")" class="btn btn-primary">Export Excel <span class="fa fa-download fa-lg" aria-hidden="true"></span></a>
    </p>



	<p>
		<a href="@Url.Action("testinggg")" class="btn btn-primary">Hour Stuff <span class="fa fa-download fa-lg" aria-hidden="true"></span></a>
	</p>

}

<p>
    @Html.ActionLink("Debug Test", "testingButtonAgain")
</p>


<!--p>
    <!-- aaasas@Html.ActionLink("Export Word", "ExportToWord")
<p-->

<section class="content-header">
    <h1>
        Data Table
        <small>Users</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li><a href="#">Tables</a></li>
        <li class="active">Data tables</li>
    </ol>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Users</h3>
                </div><!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Confirmed Email</th>
                                <th>Lockout End</th>
                                <th>Account Lockout</th>
                                <th>Login Attempts</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.UserName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EmailConfirmed)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.LockoutEndDateUtc)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.LockoutEnabled)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.AccessFailedCount)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" })
                                        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
                                        <a href="@Url.Action("Delete", new { id = item.Id})" class="btn btn-danger"><span class="fa fa-trash-o fa-lg" aria-hidden="true"></span></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Confirmed Email</th>
                                <th>Lockout End</th>
                                <th>Account Lockout</th>
                                <th>Login Attempts</th>
                                <th>Actions</th>
                            </tr>
                        </tfoot>

                    </table>
                </div><!-- /.box-body -->
            </div><!-- /.box -->
        </div><!-- /.col -->
    </div><!-- /.row -->
</section><!-- /.content -->